version: '3.8'

services:
  api:
    build:
      context: .
      dockerfile: apps/api/Dockerfile
    container_name: api-ecokids
    restart: always
    environment:
      NODE_ENV: production
      DATABASE_URL: ${DATABASE_URL}
      SERVER_PORT: ${SERVER_PORT}
      CORS_ORIGIN: ${CORS_ORIGIN}
      JWT_SECRET: ${JWT_SECRET}
      COOKIE_SECRET: ${COOKIE_SECRET}
      R2_ENDPOINT: ${R2_ENDPOINT}
      R2_PUBLIC_URL: ${R2_PUBLIC_URL}
      R2_BUCKET_NAME: ${R2_BUCKET_NAME}
      R2_ACCESS_KEY_ID: ${R2_ACCESS_KEY_ID}
      R2_SECRET_ACCESS_KEY: ${R2_SECRET_ACCESS_KEY}
    networks:
      - ecokids-network
      - traefik-public
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=traefik-public"
      - "traefik.http.routers.ecokids.rule=Host(`api.ecokids.site`)"
      - "traefik.http.routers.ecokids.entrypoints=websecure"
      - "traefik.http.routers.ecokids.tls.certresolver=letsencrypt"
      - "traefik.http.services.ecokids.loadbalancer.server.port=3333"
    deploy:
      resources:
        limits:
          memory: 1G
          cpus: "0.5"

networks:
  ecokids-network:
    external: true
  traefik-public:
    external: true
